export interface User {  id: string;  username: string;  fullName: string;  role: 'MANAGER' | 'ADMIN' | 'WORKER';  createdAt: string;  lastLogin?: string;}export interface POL {  id: string;  polNumber: string;  customerName: string;  orderDate: string;  deliveryDate: string;  status: 'PENDING' | 'IN_PROGRESS' | 'COMPLETED' | 'CANCELLED';  notes?: string;  createdBy: string;  createdAt: string;  updatedAt: string;  details?: POLDetail[];}export interface POLDetail {  id: string;  polId: string;  productCode: string;  productName: string;  quantity: number;  productType: string;  color?: string;  texture?: string;  material?: string;  size?: string;  finalSize?: string;  notes?: string;  createdAt: string;  updatedAt: string;}export interface ProductionRecord {  id: string;  polDetailId: string;  stage: 'FORMING' | 'FIRING' | 'GLAZING' | 'QUALITY_CONTROL' | 'PACKAGING';  quantity: number;  userId: string;  notes?: string;  createdAt: string;  updatedAt: string;}export interface DecorationTask {  id: string;  polDetailId: string;  taskName: string;  description?: string;  quantity: number;  completed: boolean;  userId?: string;  completedAt?: string;  createdAt: string;  updatedAt: string;}export interface Alert {  id: string;  polId: string;  polDetailId: string;  stage: 'FORMING' | 'FIRING' | 'GLAZING' | 'QUALITY_CONTROL' | 'PACKAGING';  expectedQuantity: number;  actualQuantity: number;  difference: number;  priority: 'LOW' | 'MEDIUM' | 'HIGH';  status: 'OPEN' | 'ACKNOWLEDGED' | 'RESOLVED';  reportedBy: string;  acknowledgedBy?: string;  acknowledgedAt?: string;  resolvedBy?: string;  resolvedAt?: string;  resolutionNotes?: string;  createdAt: string;  updatedAt: string;}export interface LogbookEntry {  id: string;  polId?: string;  polDetailId?: string;  userId: string;  entryDate: string;  status: 'NORMAL' | 'ISSUES' | 'RESOLVED';  notes: string;  issues?: string;  actions?: string;  createdAt: string;  updatedAt: string;}export interface RevisionTicket {  id: string;  polId: string;  polDetailId?: string;  createdBy: string;  type: 'DESIGN' | 'PRODUCTION' | 'MATERIAL' | 'OTHER';  issueType: string;  severity: 'LOW' | 'MEDIUM' | 'HIGH';  description: string;  proposedSolution?: string;  status: 'DRAFT' | 'PENDING_APPROVAL' | 'APPROVED' | 'REJECTED';  submittedAt?: string;  approvedBy?: string;  approvedAt?: string;  managerNotes?: string;  createdAt: string;  updatedAt: string;}export interface DashboardStats {  total_pols: number;  active_pols: number;  completed_this_month: number;  delayed_pols: number;  critical_alerts: number;  warning_alerts: number;  info_alerts: number;  pols_by_status: { status: string; count: number }[];  production_progress: { stage: string; progress: number }[];  recent_alerts?: Alert[];}